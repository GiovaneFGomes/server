# CONFIG REPOSITORY

#server:
#  port: 8888

#spring:
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/GiovaneFGomes/spring-cloud-config


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GiovaneFGomes/spring-cloud-config
        bootstrap: true
server:
  port: 9028
  servlet:
    context-path: /config-server